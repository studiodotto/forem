<a href="/" class="site-logo desk-header" aria-label="<%= community_name %> Home">
  <% if SiteConfig.logo_svg.present? %>
    <%= logo_svg %>
  <% else %>
    <span class="truncate-at-2">
      <%= community_name %>
    </span>
  <% end %>
</a>
